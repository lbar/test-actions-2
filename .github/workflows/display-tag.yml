name: Listen for Tags

on:
  workflow_run:
    workflows: ["Push Tag"]
    types: [completed]
  push:
    tags:
      - '*'

jobs:
  listen-for-tags:
    runs-on: ubuntu-latest

    steps:
      - name: Afficher le tag
        run: |
          echo "Le tag poussé est ${{ github.ref }}"
